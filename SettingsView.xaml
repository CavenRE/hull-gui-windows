<UserControl x:Class="Hull.Gui.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Settings" Style="{StaticResource ScreenTitle}" Margin="24,20,24,8" />
        <Border Grid.Row="1" x:Name="TabsHost" Margin="24,0,24,0" />
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="24,12,24,28">
            <StackPanel x:Name="Body" />
        </ScrollViewer>
    </Grid>
</UserControl>

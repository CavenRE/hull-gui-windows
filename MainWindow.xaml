<Window x:Class="Hull.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hull" Height="640" Width="940"
        Background="#242423" FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="Pill" TargetType="Button">
            <Setter Property="Background" Value="#3A3A38" />
            <Setter Property="Foreground" Value="#E8EDDF" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Top bar -->
        <Border DockPanel.Dock="Top" Background="#1C1C1B" Padding="16,12">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Hull" Foreground="#F5CB5C" FontSize="20" FontWeight="Bold" />
                    <TextBlock x:Name="StatusText" Text="Connecting…" Foreground="#A8A89F"
                               Margin="14,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Refresh" Style="{StaticResource Pill}" Click="OnRefresh" />
                    <Button Content="Stop All" Style="{StaticResource Pill}" Click="OnStopAll" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Project list -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12">
            <ItemsControl x:Name="ProjectList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2E2E2C" CornerRadius="6" Padding="14,10" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding name}" Foreground="#E8EDDF"
                                                   FontSize="15" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding kind}" Foreground="#8A8A82"
                                                   Margin="10,2,0,0" FontSize="12" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                        <TextBlock Text="{Binding State}" Foreground="#A8A89F" FontSize="12" />
                                        <TextBlock Text="{Binding url}" Foreground="#6Fae6f"
                                                   Margin="12,0,0,0" FontSize="12" />
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Start" Style="{StaticResource Pill}" Click="OnStart" />
                                    <Button Content="Stop" Style="{StaticResource Pill}" Click="OnStop" />
                                    <Button Content="Restart" Style="{StaticResource Pill}" Click="OnRestart" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
